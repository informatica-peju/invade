# Arquivo de inventário JSON com múltiplos roteadores
INVENTORY_FILE=configs/inventory.json

# Defaults opcionais (usados se não vierem por roteador)
ROUTER_USER=admin
ROUTER_PASSWORD=
ROUTER_PORT=22

# Timeout SSH
SSH_TIMEOUT=20
